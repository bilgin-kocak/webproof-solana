# TLSNotary spike (release-blocked)

This directory is reserved for Stage 1. It deliberately contains no fake HTTPS fallback. See `docs/tlsnotary-version.md`; pinning and compiling a current official example is mandatory before this crate may be described as working.
